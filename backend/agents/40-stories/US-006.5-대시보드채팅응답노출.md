# US-006.5 대시보드 채팅 응답 노출

## 시나리오
1. 사용자가 대시보드에서 질문을 전송한다.
2. 전송 직후 입력한 질문 버블이 즉시 리스트에 렌더링된다.
3. 백엔드 `/api/aiagent/search` 응답이 완료되면 동일 스레드에 답변이 붙는다.
4. 사용자가 다른 페이지로 이동했다 돌아오거나 새로고침해도 질문과 답변이 모두 그대로 복원된다.

## 결과
- 질문이 누락되거나 답변만 남는 현상 없이 사용자-응답 쌍이 항상 보존된다.
- 백엔드 응답이 도착하면 리프레시 없이도 즉시 UI에 반영된다.
